data:
  batch_size: 32
  casia_root: ${data.datasets.root}/Real/CASIA-WebFace/imgs
  casia_train: true
  casia_val_n_classes: 200
  max_matches_per_image: 3
  max_nonmatches_per_image: 3
  augment: true
  num_workers: 4

model:
  backbone: ir_50
  n_classes: 10372
  margin: 0.4
  h: 0.333
  s: 64.0
  t_alpha: 1.0
  distance_fn: euclidean
  lr: 0.1
  momentum: 0.9
  lr_milestones: [8, 12, 14]
  lr_gamma: 0.1

trainer:
  max_epochs: 50
  precision: bf16-mixed